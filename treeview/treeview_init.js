$(document).ready(function(){
	// second example
	$("#navigation").treeview({
	    animated: "slow",
		collapsed: true,
		unique: true,
		persist: "location"
	});
	$("#navigation_menu").treeview({
	    animated: "slow",
		collapsed: true,
		unique: true,
		persist: "location"
	});
});
